package com.example.android.BluetoothChat;

import android.app.Activity;
import android.app.NotificationManager;
import android.content.Intent;

public class StopBluetoothService extends Activity {
	//@Override
    public void onCreate() {
    	//stopForeground(true);
    	//NotificationManager mNM = (NotificationManager) getSystemService(NOTIFICATION_SERVICE);
    	//mNM.cancel(1002);
    	//Intent i = new Intent();
    	//i.setComponent(new Intent(this,ServiceClass.class)));
    	//finish();
    	//stopService(new Intent("com.example.android.BluetoothChat.FOREGROUND"));
    	//stopService(new Intent(this,ServiceClass.class));
	}
}
